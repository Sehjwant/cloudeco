# Cognito (fill these in after creating the User Pool + App Client)
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_AWS_REGION=us-east-1

# Backend REST API base URL (teammates' API Gateway / secondary-cloud endpoint).
# Leave blank until the backend exists — the UI degrades gracefully without it.
VITE_API_BASE_URL=
